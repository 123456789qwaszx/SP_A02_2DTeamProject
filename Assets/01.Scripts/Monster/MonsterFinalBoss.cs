using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MonsterFinalBoss : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
    // Dead 애니메이션 이벤트 함수
    //public void OnDeadEnd()
    //{
    //    itemDropManager.TryDropItem(transform.position);
    //    PoolManager.Instance.Push(this.gameObject);

    //    if (monsterData.type == MonsterData.MonsterType.Boss ||
    //        monsterData.type == MonsterData.MonsterType.FinalBoss)
    //    {
    //        StageManager.Instance.CompleteStage(); // 보스 죽으면 바로 클리어
    //    }
    //}

}
